const type = ["android", "iOS", "Web"]
console.log(" Slice : Slice out a part of an array starting from array element : " + type.slice(1))


const num = [1, 2, 3, 4, 5, 6, 7, 8, 9];
const res = num.slice(2, 5); // Include element of index 2 but exclude all the elements from index 5 
console.log(res);

